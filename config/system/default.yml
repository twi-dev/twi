###
# Do not edit this file!
# Put your settings to env-specific file.
# For example: use development.yml for development env.
#
# Note: Environment can be set using the NODE_ENV= variable in shell, like so:
# $ NODE_ENV=production node .
###

# Name of your app.
name: Twilight's Library

# Is sign up ebable on your site?
# Can be only "true" or "false"
enableSignup: true

server:
  host: localhost
  port: 2319
  secure: false # Should I remove this options too?

client: null

database:
  host: localhost
  port: 27017 # Default MongoDB port
  user: root
  pass: null
  name: twi

jwt:
  accessToken:
    secret: null
    expires: 15m # Supported format of zeit/ms
  refreshToken:
    secret: null
    expires: null

# Static server configuration
# Status: not implemented
static:
  adapter: null
  auth:
    user: null
    pass: null
  options: null


# Email client configuration
mail:
  from: Postmare # Human-readable name of the sender
  host: null # Hostname of your SMTP server to connect to
  port: null #Port of your SMTP server to connect to
  auth:
    type: null
    user: null
    pass: null
  authMethod: null

  # Or just provide a service name, if you're using something like Yandex or Google
  # You can find more information in Nodemailer's docs
  service: null

  # Use SSL?
  # Can be only "true" of "false"
  secure: true
