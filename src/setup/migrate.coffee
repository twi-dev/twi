fs = require "promise-fs"
db = require "../core/database"
requireHelper = require "../core/helper/require"

importData = (clean = off) ->

migrate = (clean = off) -> console.log "migrate"

module.exports = migrate
