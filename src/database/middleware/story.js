// import invariant from "@octetstream/invariant"

// import {before} from "core/database/middleware"

// async function beforeSave(story) {
//   invariant(
//     story.translation.flag && !story.translation.author,
//     "Author's information required for translations"
//   )
// }

// module.exports = {
//   beforeSave: before.save(beforeSave)
// }
